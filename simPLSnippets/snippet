func slow_pow(base, exp) {
    result := 1;
    while (exp > 0) {
        result = result * base;
        exp = exp - 1;
    }
    return result;
}

func ugly_factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return factorial(n - 1) * n;
}
