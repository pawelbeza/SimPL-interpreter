set(HEADER
        ${HEADER}
        ${CMAKE_CURRENT_SOURCE_DIR}
        PARENT_SCOPE
        )

set(SOURCE
        ${SOURCE}
        ${CMAKE_CURRENT_SOURCE_DIR}/Token.cpp
        ${CMAKE_CURRENT_SOURCE_DIR}/TokenTypeFactory.cpp
        PARENT_SCOPE
        )

